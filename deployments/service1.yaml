apiVersion: v1
kind: Service
metadata:
  name: hostname-svc
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30001
      protocol: TCP
  selector:
    app: hostname-app
